{
  "name": "@brs/contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The BRS content contract. Versioned DTOs + typed client. The ONLY data surface any frontend may know.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "generate": "tsx src/generate-openapi.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
