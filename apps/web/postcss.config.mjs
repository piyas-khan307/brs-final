/** Tailwind 4 uses a PostCSS plugin; configuration is CSS-first in
 *  src/app/globals.css via @theme (implementation_plan.md §10.4). */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
